---
const tinycssBaseURL = "https://cdn.jsdelivr.net/npm/tiny.css@0";

const tinycssAutoURL  = `${tinycssBaseURL}/dist/tiny.css`;
const tinycssDarkURL  = `${tinycssBaseURL}/dist/dark.css`;
const tinycssLightURL = `${tinycssBaseURL}/dist/light.css`;

---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <meta property="og:title" content="Tiny.css" />
    <meta property="og:description" content="A very minimalistic and beautiful css library." />

    <title>Tiny.css</title>
    <link rel="shortcut icon" href="https://raw.githubusercontent.com/ihsan6133/tiny.css/main/assets/logo.svg" type="image/x-icon">
    
    <link rel="preload" as="style" href={tinycssLightURL}>
    <link rel="preload" as="style" href={tinycssDarkURL}>


    <link rel="stylesheet" id="stylesheet-link" href={tinycssAutoURL}>

    <style>
        /* :root {
            --background-body: hsl(200, 30%, 14%);
            --background: hsl(200, 26%, 21%);
            --background-hover: hsl(200, 23%, 29%);

            --button-base: hsl(200, 26%, 21%);
            --button-hover: hsl(200, 23%, 29%);
        } */

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            height: 4.5rem
        }

        /* For github logo */
        svg path {
            fill: #777;
        }
        @media (prefers-color-scheme: dark) {
            img {
                filter: drop-shadow(0px 0px 2px rgba(0, 0, 0, 0.5));
            }
        }

        .theme-btn {
            padding: 0.5rem;
        }

        .theme-btn.active {
            border-color: #0070e0;
            background-color: #0070e0;
            color: white;
        }
        .theme-btn.active:hover {
            border-color: #0050a0;
            background-color: #0050a0;
        }
    </style>
    <script define:vars={{ tinycssAutoURL, tinycssLightURL, tinycssDarkURL }}>
        window.onload = () => {

            const themeBtnDark   = document.querySelector(".theme-btn.dark");
            const themeBtnLight  = document.querySelector(".theme-btn.light");
            const stylesheetLink = document.querySelector("#stylesheet-link");
            const dialog         = document.querySelector("dialog");
            const dialogBtn      = document.querySelector(".dialogBtn");
            const dialogCloseBtn = document.querySelector(".dialogCloseBtn");

            themeBtnDark.addEventListener("click", () => {
                if (themeBtnDark.classList.contains("active")) {
                    stylesheetLink.href = tinycssAutoURL;
                    themeBtnDark.classList.remove("active");
                } else {    
                    document.querySelector("#stylesheet-link").href = tinycssLightURL;
                    themeBtnDark.classList.add("active");
                    themeBtnLight.classList.remove("active");
                }
            });

            themeBtnLight.addEventListener("click", () => {
                if (themeBtnLight.classList.contains("active")) {
                    stylesheetLink.href = tinycssAutoURL;
                    themeBtnLight.classList.remove("active");
                } else {
                    document.querySelector("#stylesheet-link").href = tinycssDarkURL;
                    themeBtnLight.classList.add("active");
                    themeBtnDark.classList.remove("active");
                }  
            });

            dialogBtn.onclick = () => {
                dialog.showModal();
            }

            dialogCloseBtn.onclick = () => {
                dialog.close();
            }
            
        }
    </script>
</head>
<body>
    <header>
        <h1>Tiny.css</h1>
        <img class="logo" src="https://raw.githubusercontent.com/ihsan6133/tiny.css/main/assets/logo.svg" alt="Tiny.css Logo">
    </header>

    <p><strong>Tiny.css</strong> is a <em>very</em> minimalistic css library to override the
       horrendous default style with good looking design. If you quickly
       want to get an html page up and running, tiny.css is for you!</p>
    
    <span style="display: flex; align-items: center; gap: 0.3rem;">
        <svg style="height: 1.3rem" viewBox="0 0 98 96" xmlns="http://www.w3.org/2000/svg">
            <path fill-rule="evenodd" clip-rule="evenodd" d="M48.854 0C21.839 0 0 22 0 49.217c0 21.756 13.993 40.172 33.405 46.69 2.427.49 3.316-1.059 3.316-2.362 0-1.141-.08-5.052-.08-9.127-13.59 2.934-16.42-5.867-16.42-5.867-2.184-5.704-5.42-7.17-5.42-7.17-4.448-3.015.324-3.015.324-3.015 4.934.326 7.523 5.052 7.523 5.052 4.367 7.496 11.404 5.378 14.235 4.074.404-3.178 1.699-5.378 3.074-6.6-10.839-1.141-22.243-5.378-22.243-24.283 0-5.378 1.94-9.778 5.014-13.2-.485-1.222-2.184-6.275.486-13.038 0 0 4.125-1.304 13.426 5.052a46.97 46.97 0 0 1 12.214-1.63c4.125 0 8.33.571 12.213 1.63 9.302-6.356 13.427-5.052 13.427-5.052 2.67 6.763.97 11.816.485 13.038 3.155 3.422 5.015 7.822 5.015 13.2 0 18.905-11.404 23.06-22.324 24.283 1.78 1.548 3.316 4.481 3.316 9.126 0 6.6-.08 11.897-.08 13.526 0 1.304.89 2.853 3.316 2.364 19.412-6.52 33.405-24.935 33.405-46.691C97.707 22 75.788 0 48.854 0z" fill="#24292f"/>
        </svg>

        <p>Visit the github repository <a href="https://github.com/ihsan6133/tiny.css">here</a>!</p>
    </span>
    
    <h2>Installation</h2>

    <p>Tiny.css is very simple to start using! Just add the following inside your <code>&lt;head&gt;</code> tag:</p>
    <pre><code>&lt;link rel="stylesheet" href="{tinycssAutoURL}"&gt;</code></pre>

    <p>This selects dark or light theme automatically by detecting system preference using <code>prefers-color-scheme</code> css media query. Alternatively, if you want only a specific theme, you can use the following:</p>
    <p><button class="theme-btn dark" title="Click to toggle">Light theme ☀️</button> :</p>
    <pre><code>&lt;link rel="stylesheet" href="{tinycssLightURL}"&gt;</code></pre>

    <p><button class="theme-btn light" title="Click to toggle">Dark theme 🌙</button> :</p>
    <pre><code>&lt;link rel="stylesheet" href="{tinycssDarkURL}"&gt;</code></pre>

    <h2>Customizability</h2>
    
    <p>Since tiny.css is structured with many top level variables for colors, so you can easily customize the color schemes by overriding the variables! Here are all the variables you can tweak:</p>
    
    <ul>
        <li><code>--background-body</code></li>
        <li><code>--background</code></li>
        <li><code>--background-hover</code></li>
        <li><code>--text-main</code></li>
        <li><code>--text-bright</code></li>
        <li><code>--text-muted</code></li>
        <li><code>--button-base</code></li>
        <li><code>--button-hover</code></li>
        <li><code>--links</code></li>
        <li><code>--focus</code></li>
        <li><code>--table</code></li>
        <li><code>--table-bg</code></li>
        <li><code>--table-bg-alt</code></li>
        <li><code>--box-shadow</code></li>
    </ul>


    <h2>Demos</h2>

    <input type="text" placeholder="Name">
    <br>

    <pre><code>// This is Javascript <br>console.log("Hello World!");</code></pre>
    
    <blockquote>
        The HTML blockquote Element (or HTML Block Quotation Element)
        indicates that the enclosed text is an extended quotation.
        Usually, this is rendered visually by indentation (see Notes
        for how to change it). A URL for the source of the quotation
        may be given using the cite attribute, while a text 
        representation of the source can be given using the 
        <code>&lt;cite&gt;</code> element.
        
        <footer>
            <cite>MDN, "The Block Quotation Element"</cite>
        </footer>
    </blockquote>

    <button class="dialogBtn">Click me for dialog!</button>
    <dialog>
        <header>This is a dialog</header>
        <button class="dialogCloseBtn">Close</button>
    </dialog>
    <details>
        <summary>This is a sumary!</summary>
        <p>These are the details</p>
    </details>

    <p>Here is a really nice table! </p>
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Quantity</th>
                <th>Price</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>Apple</td>
                <td>2</td>
                <td>$2</td>
            </tr>
            <tr>
                <td>Orange</td>
                <td>3</td>
                <td>$3</td>
            </tr>
            <tr>
                <td>Banana</td>
                <td>4</td>
                <td>$4</td>
            </tr>
        </tbody>
    </table>

    <p>Here is an Image:</p>

    <img src="https://placekitten.com/600/400" alt="Cat Picture">

</body>
</html>

